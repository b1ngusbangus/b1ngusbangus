function moveCat() {

    var image = document.getElementById('movingCat');
    image.classList.add('animation');

    // var image = document.getElementById('movingImage')
    // var container = document.getElementById('container');
    // var containerWidth = container.offsetWidth;
    // var containerHeight = container.offsetHeight;

    // var newX = Math.random() * (containerWidth - image.width);
    // var newY = Math.random() * (containerHeight - image.height);

    // image.style.transform = "translate(" + newX + "px, " + newY + "px)";
}


